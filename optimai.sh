sudo apt update && curl -fsSL https://get.docker.com | sudo sh && sudo systemctl enable --now docker && sudo usermod -aG docker $USER && curl -L https://optimai.network/download/cli-node/linux -o optimai-cli && chmod +x optimai-cli && sudo mv optimai-cli /usr/local/bin/ && echo -e "\n\n=== CAI DAT XONG. HAY NHAP EMAIL VA PASSWORD CUA BAN BEN DUOI ===\n" && sg docker -c "optimai-cli auth login && screen -dmS optimai optimai-cli node start && echo -e '\n=== DA KHOI CHAY NODE THANH CONG TRONG SCREEN ==='"
screen -r optimai
